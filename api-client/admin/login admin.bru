meta {
  name: login admin
  type: http
  seq: 1
}

post {
  url: {{host}}/admin/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@supreme.dev",
    "password": "password"
  }
}

settings {
  encodeUrl: true
}
